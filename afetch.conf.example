# Example configuration file for afetch
# Copy this file to afetch.conf and modify the values below

# GitHub API configuration
# Generate a personal access token at https://github.com/settings/tokens
# Token is required for private repositories, optional for public repositories
GITHUB_TOKEN="your_github_token_here"

# Repository owner (username or organization name)
REPO_OWNER="your_repo_owner_here"

# Repository name
REPO_NAME="your_repo_name_here"

# Asset mask (glob pattern to match asset filenames)
# If empty or commented out, the application will show release selection mode
# where you can first select a release, then choose from all its assets

# Examples of asset masks:
# ASSET_MASK="*.zip"              # Match all .zip files
# ASSET_MASK="*.tar.gz"           # Match all .tar.gz files
# ASSET_MASK="myapp-*"            # Match files starting with "myapp-"
# ASSET_MASK="*-linux-x64*"       # Match files containing "-linux-x64"
# ASSET_MASK="*.AppImage"         # Match all AppImage files

ASSET_MASK="*.your_extension_here"

# Leave ASSET_MASK empty to enable release selection mode:
# ASSET_MASK=""
